<h5>Xóa hệ số 0.2</h5>               

<div class="main-content-box"> 
    <?php if ($this->error_message) : ?>
        <div class="alert alert-error">
            <button type="button" class="close" data-dismiss="alert">&times;</button>
            <h5>Thông báo lỗi!</h5>                
            <ol>
                <?php foreach ($this->error_message as $message): ?>
                    <li><?php echo $message; ?></li>
                <?php endforeach; ?>
            </ol>
        </div> 
    <?php endif; ?>          
    <?php if ($this->he_so) : ?>        
        <form class="form-horizontal" method="post" action="">
            <?php echo $this->formHidden('id', $this->he_so->id); ?>
            <div class="control-group">
                <div class="controls">
                    <p>
                        Bạn có chắc chắn muốn xóa hệ số 0.2 năm <strong><?php echo $this->he_so->nam; ?></strong>
                    </p>
                </div>
            </div>
            <div class="control-group">
                <div class="controls">
                    <p>
                        <button name="del" value="Yes" class="btn btn-small btn-primary" type="submit">Đồng ý xóa</button>
                        <a class="btn btn-small" type="button" href="<?php echo $this->baseUrl('taivu/heso02/index/page/' . $this->arrParam['page']); ?>">Quay lại</a>
                    </p>
                </div>
            </div>
        </form>
    <?php else : ?>
        <form class="form-horizontal" method="post" action="">
            <div class="control-group">
                <div class="controls">
                    <p>
                        <a class="btn btn-small" type="button" href="<?php echo $this->baseUrl('taivu/heso02/index/page/' . $this->arrParam['page']); ?>">Quay lại</a>
                    </p>
                </div>
            </div>
        </form>
    <?php endif; ?>        
</div> 